值对象（Value Object）还是实体（Entity）？

折扣规则本身无独立生命周期，依附于商家

由属性（类型type、门槛threshold、优惠金额discountAmount）定义其身份

不可变性（规则变更应创建新规则，不修改旧规则）

结论：折扣规则是值对象

聚合归属

折扣规则属于商家的配置信息

但订单计算时需要引用折扣规则

建议：折扣规则作为独立的聚合根或值对象集合，由商家聚合管理

扩展性设计

使用接口分离折扣类型（interface Discount）

折扣类型使用枚举

持久化设计
仿照已有的地址和价格信息的设计，考虑将订单所用折扣信息嵌入订单表
